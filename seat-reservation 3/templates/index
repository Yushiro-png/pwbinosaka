<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>Piano with Ballet 東京公演</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Google Fonts & Bootstrap -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@500;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* ===== 全体背景に映画の照明風グラデーション ===== */
        body {
            font-family: 'Noto Serif JP', serif;
            background: linear-gradient(135deg, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0.1) 25%, rgba(255,255,255,0.2) 50%, rgba(255,255,255,0.05) 75%, rgba(255,255,255,0) 100%), #f8f9fa;
            color: #333;
            position: relative;
            overflow-x: hidden;
        }

        h2 {
            text-align: center;
            font-weight: 700;
            color: #6c1e8f;
            margin-bottom: 2rem;
        }
        h5 {
            color: #5c2a8c;
        }

        .performance-details {
            background-color: #fff;
            padding: 2rem;
            border-radius: 1rem;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            margin-bottom: 3rem;
            position: relative;
            overflow: hidden;
        }

        .performance-details ul {
            padding-left: 0;
            list-style: none;
        }
        .performance-details .col-md-6 {
            padding-left: 1.5rem;
            padding-right: 1.5rem;
        }
        .performance-details li strong {
            color: #6c1e8f;
        }

        .carousel-item img {
            width: 50%;
            object-fit: cover;
            margin: auto;
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
            position: relative;
            z-index: 1;
        }

        .carousel-item img::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 20% 20%, rgba(255, 255, 255, 0.15), transparent 80%);
            pointer-events: none;
            border-radius: 15px;
        }

        .carousel-inner {
            text-align: center;
        }

        .btn-primary {
            background-color: #6c1e8f;
            border: none;
        }
        .btn-primary:hover {
            background-color: #5a1a77;
        }

        .form-control, .form-select {
            border-radius: 0.5rem;
        }

        .rounded.shadow {
            border-radius: 1rem !important;
        }

        .carousel-control-prev-icon,
        .carousel-control-next-icon {
            filter: invert(1);
        }

        a {
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }

        hr {
            border-top: 2px solid #ccc;
        }

        /* ===== 出演者カード 高級感アニメーション ===== */
        .artist-card {
            background-color: #fff;
            border-radius: 1rem;
            box-shadow: 0 4px 20px rgba(0,0,0,0.15);
            padding: 1rem;
            overflow: hidden;
            position: relative;
            transition: transform 0.5s ease-out, box-shadow 0.5s ease-out;
        }

        .artist-card img {
            width: 200px;
            height: auto;
            display: block;
            margin: 0 auto;
            border-radius: 1rem;
            transform: scale(1.2);
            opacity: 0;
            transition: all 1s cubic-bezier(0.25, 0.1, 0.25, 1);
        }

        .artist-text {
            opacity: 0;
            transform: translateY(40px);
            transition: all 1s cubic-bezier(0.25, 0.1, 0.25, 1);
        }

        /* 表示されたとき */
        .artist-card.show img {
            transform: scale(1);
            opacity: 1;
        }

        .artist-card.show .artist-text {
            transform: translateY(0);
            opacity: 1;
        }

        /* 遅延設定 */
        .artist-card:nth-child(1) img { transition-delay: 0.2s; }
        .artist-card:nth-child(1) .artist-text { transition-delay: 0.4s; }

        .artist-card:nth-child(2) img { transition-delay: 0.4s; }
        .artist-card:nth-child(2) .artist-text { transition-delay: 0.6s; }

        .artist-card:nth-child(3) img { transition-delay: 0.6s; }
        .artist-card:nth-child(3) .artist-text { transition-delay: 0.8s; }

        /* ホバー時に浮遊感 */
        .artist-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }

        /* ===== 光のスポットフェード ===== */
        .performance-details::before,
        .artist-card::before,
        form::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle at top left, rgba(255, 255, 255, 0.2), transparent 70%);
            transform: rotate(25deg);
            pointer-events: none;
            opacity: 0;
            transition: opacity 2s ease-in-out;
        }

        .performance-details.show::before,
        .artist-card.show::before,
        form.show::before {
            opacity: 1;
        }
    </style>
</head>
<body class="container py-4">

    <h2>Piano with Ballet 東京公演</h2>

    <!-- 公演詳細 -->
    <div class="performance-details row mx-auto">
        <h5 class="text-center fw-bold mb-4 w-100">🎼 公演詳細</h5>
        <div class="col-md-6">
            <ul>
                <li><strong>日時：</strong>2026年8月15日（土）</li>
                <li><strong>開場：</strong>13:00　　<strong>開演：</strong>14:00</li>
                <li><strong>会場：</strong>かつしかシンフォニーヒルズ アイリスホール</li>
                <li><strong>住所：</strong>〒124-0012 東京都葛飾区立石6丁目33-1</li>
                <li class="mt-2">
                    <a href="https://maps.app.goo.gl/ijcV3j93aqgGoDiq8" target="_blank" class="btn btn-outline-secondary btn-sm">
                        📍 Googleマップで開く
                    </a>
                </li>
                <li><strong>お問い合わせ先：</strong></li>
                <ul class="list-unstyled ms-3">
                    <li class="mb-2">
                        <a href="mailto:pianowithballetintokyo@gmail.com" class="btn btn-outline-primary btn-sm rounded-pill shadow-sm">
                            📧 メール
                        </a>
                    </li>
                    <li>
                        <a href="https://www.instagram.com/yushiro03" target="_blank" class="btn btn-outline-danger btn-sm rounded-pill shadow-sm">
                            📷 Instagram
                        </a>
                    </li>
                </ul>
            </ul>
        </div>
        <div class="col-md-6">
            <ul>
                <li><strong>料金：</strong></li>
                <ul>
                    <li>🎫 <strong>VIP席（A3〜A17）</strong>：8,000円（振込割引なし）</li>
                    <li>🎫 <strong>S席（B2〜I19）</strong>：
                        <ul>
                            <li>大人：6,000円</li>
                            <li>学生：5,000円</li>
                            <li>中学生以下：4,000円</li>
                        </ul>
                    </li>
                    <li>🎫 <strong>A席（J1〜P19）</strong>：
                        <ul>
                            <li>大人：4,000円</li>
                            <li>学生：3,000円</li>
                            <li>中学生以下：2,000円</li>
                        </ul>
                    </li>
                </ul>
                <li class="mt-3 text-danger"><strong>※事前振込で500円割引（VIP席を除く）</strong></li>
                <li class="mt-2 text-danger"><strong>※まとめてのご予約はできません。必ずお一人ずつお申し込みください。</strong></li>
            </ul>
        </div>
    </div>

    <!-- 座席表＋カルーセル -->
    <div class="row mb-5">
        <div class="col-12">
            <h5 class="mb-3 text-center fw-bold">座席表</h5>
            <img src="{{ url_for('static', filename='seating_chart.jpg') }}" class="img-fluid mb-4 rounded shadow d-block mx-auto" alt="座席表">

            <h5 class="mt-5 mb-3 text-center fw-bold border-bottom pb-2">過去のコンサートの様子</h5>
            <div id="concertCarousel" class="carousel slide mb-4" data-bs-ride="carousel">
                <div class="carousel-indicators">
                    <button type="button" data-bs-target="#
